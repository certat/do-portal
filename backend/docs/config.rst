Configuration defaults
======================

To override configuration defaults set the :envvar:`DO_LOCAL_CONFIG` environment
variable.

.. automodule:: config
    :members:
    :show-inheritance:

The configuration resides in `config.py` in the class `Config` as members.
